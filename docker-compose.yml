services:
  db1:
    # command: -c config_file=/etc/postgresq/postgresql.conf    
    build:
      context: db1
      dockerfile: Dockerfile
    ports:
      - 55432:5432
    environment:
      POSTGRES_PASSWORD: password
  db2:
    build:
      context: db2
      dockerfile: Dockerfile
    ports:
      - 55433:5432
    environment:
      POSTGRES_PASSWORD: password
